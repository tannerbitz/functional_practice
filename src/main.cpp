#include "FileLength.h"
#include <iostream>
#include <vector>
// #include <boost/filesystem>


int main(int argc, char** argv)
{
    // const auto curr_dir = boost::filesystem::current_path();
    // std::cout << curr_dir.string() << std::endl;
    return 0;
}